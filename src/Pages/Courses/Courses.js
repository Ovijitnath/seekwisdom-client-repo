import React, { useState } from 'react';

const Courses = () => {
    const [categories, setCategories] = useState([]);
    return (
        <div>
            <h1>Total Courses: </h1>
        </div>
    );
};

export default Courses;